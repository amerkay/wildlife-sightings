import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  // content: [
  // 	'./components/**/*.{vue,js,ts}',
  // 	'./layouts/**/*.vue',
  // 	'./pages/**/*.vue',
  // 	'./app.vue',
  // 	'./plugins/**/*.{js,ts}',
  // ],
  // theme: {
  // 	extend: {
  // 		typography: {
  // 			DEFAULT: {
  // 				css: {
  // 					color: 'var(--foreground)',
  // 					textAlign: 'left',
  // 					a: {
  // 						color: 'var(--accent)',
  // 						textDecoration: 'none',
  // 						'&:hover': {
  // 							textDecoration: 'underline',
  // 						},
  // 					},
  // 					p: {
  // 						fontFamily: 'var(--theme-font-family-body)',
  // 					},
  // 					figcaption: {
  // 						fontSize: 'clamp(0.75em, 1vw, 1rem) !important',
  // 						marginTop: '0.75rem !important',
  // 					},
  // 					li: {
  // 						'& p': {
  // 							display: 'inline',
  // 							margin: '0',
  // 						},
  // 					},
  // 					blockquote: {
  // 						fontStyle: 'italic',
  // 						borderLeft: '4px solid var(--ring)',
  // 						textAlign: 'left',
  // 					},
  // 				},
  // 			},
  // 			dark: {
  // 				css: {
  // 					color: 'var(--foreground)',
  // 					a: {
  // 						color: 'var(--accent)',
  // 						textDecoration: 'none',
  // 						'&:hover': {
  // 							textDecoration: 'underline',
  // 						},
  // 					},
  // 					blockquote: {
  // 						borderLeftColor: 'var(--ring)',
  // 					},
  // 				},
  // 			},
  // 		},
  // 	},
  // },
  // plugins: [typography],
};

export default config;
